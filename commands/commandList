atrib;adds an attribute to the currently selected XML node. (format: atrib name val)
leaf;adds an XML node that has a value content. (format: leaf tagName value) //(An XML leaf node can not contain other XML nodes)\\
container;adds an XML container node as a child of the currently selected node. (format: container tagName)
print-r;displays the currently selected root and all of the nodes nested inside of it.
print-c;displays only the current XML node you have selected.
print-a;displays the whole xml document. (all of the root nodes)
root;adds a new root node in the editor. (format: root tagName)
prev-root;returns to the previous node you had selected in the previous root node.
next-root;saves the node you had last selected and goes to the next root in order.
revert;this command can be called after using the "clear" command. It reverts the document as it was before it was cleared.;permitted
clear;clears the document
change-c;changes the current node to another node in the same root. (format: change-c (tagName))
swap;swaps 2 nodes passed by their tag name (format: swap (tag1) (tag2)
down;steps inside the currently selected container node. (if possible)
up;steps outside the currently selected container node. (if possible)
next;selects the next node that is in the same level of the currently selected node.(this command is analogous to the next-root command if the currently selected element is a root element)
top;selects the current root node as the current node.
remove-c;removes the currently selected node from the XML editor.
write;writes the content that is currently in the document to a file.
commands-all;displays all of the commands that this program offers;permitted
commands-avb;displays the currently available commands;permitted
show-templates;displays currently available templates;permitted
template;loads a template xml code (format: template (name);permitted
save-template;saves the commands up until now as a template with name (name). (format: save-template (name))
delete-template;deletes the template with the name (name). (format: delete-template (name));permitted
ptog;toggles printing on/off after every command.
help;displays info about a command;permitted

